<h1>Let`s go ! Create a new Review for <%= @restaurant.name %> restaurant ! </h1>

<%= simple_form_for [@restaurant, @review] do |f|%>

  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit, class: 'btn btn-primary' %>

<% end %>
